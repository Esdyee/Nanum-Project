import { WindowScrollDirective } from './window-scroll.directive';

describe('WindowScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowScrollDirective();
    expect(directive).toBeTruthy();
  });
});
