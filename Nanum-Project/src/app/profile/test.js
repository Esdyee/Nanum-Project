const userEmploymentCredential = {
  pk: 'number',
  url: 'string',
  company: {
    name: 'string',
  },
};

console.log(Object.getOwnPropertyNames(userEmploymentCredential));
console.log(Object.keys(userEmploymentCredential));